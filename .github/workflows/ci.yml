name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20'

      - name: Set up yarn
        run: npm install --global yarn

      - name: Install dependencies
        run: yarn install

      - name: Run tests
        run: yarn test

      - name: Build project
        run: yarn run build

      # - name: Create key
      #   run: |
      #     echo "$PRIVATE_KEY" > private_key.pem && chmod 400 private_key.pem

      - name: copy file via ssh password
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 22
          source: ./package.json
          target: ${{ secrets.EC2_APP_DIRECTORY }}

      # - name: Send package.json
      #   run: |
      #     scp -i private_key.pem ./package.json ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:${{ secrets.EC2_APP_DIRECTORY }}

      # - name: Send files
      #   run: |
      #     scp -i ./private_key.pem -r ./dist/* ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }}:${{ secrets.EC2_APP_DIRECTORY }}

      # - name: Deploy to EC2
      #   run: |
      #     ssh ${{ secrets.EC2_USER }}@${{ secrets.EC2_HOST }} 'bash -s' << 'EOF'
      #     cd ${{ secrets.EC2_APP_DIRECTORY }}
      #     yarn install --production
      #     yarn run start:prod
      #     EOF
